# ── Core ──────────────────────────────────────────────────────────────────────
GOOGLE_API_KEY=your-google-api-key

# ── Slack Integration ─────────────────────────────────────────────────────────
# Create a Slack app at https://api.slack.com/apps
# 1. Enable Socket Mode → generate an App-Level Token (xapp-…)
# 2. Install the app → copy the Bot User OAuth Token (xoxb-…)
# 3. Add Slash Commands: /earnings   (request URL not needed for Socket Mode)
# 4. Subscribe to Events: app_mention
SLACK_BOT_TOKEN=xoxb-your-bot-token
SLACK_APP_TOKEN=xapp-your-app-token

# ── Email Integration — SMTP (outbound) ───────────────────────────────────────
# Gmail example: host=smtp.gmail.com, port=587, use app password if 2FA enabled
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_USE_TLS=true
EMAIL_FROM_NAME=NotionWealth Intelligence

# ── Email Integration — IMAP (inbound query polling) ─────────────────────────
# Analysts send an email to IMAP_USER; subject = company query; bot replies
IMAP_HOST=imap.gmail.com
IMAP_PORT=993
IMAP_USER=your-email@gmail.com
IMAP_PASS=your-app-password
IMAP_POLL_INTERVAL_SECONDS=60

# ── Supabase ──────────────────────────────────────────────────────────────────
# Project URL from Settings → API
SUPABASE_URL=https://your-project-ref.supabase.co
# service_role key (keep secret — server-side only)
SUPABASE_SERVICE_ROLE_KEY=eyJ...your-service-role-key
# JWT secret from Settings → API → JWT Settings
SUPABASE_JWT_SECRET=your-jwt-secret

# ── Google OAuth (for per-user Gmail) ────────────────────────────────────────
# Create credentials at console.cloud.google.com → APIs & Services → Credentials
# Scopes: gmail.send, gmail.readonly, userinfo.email
GOOGLE_CLIENT_ID=your-google-oauth-client-id
GOOGLE_CLIENT_SECRET=your-google-oauth-client-secret
# Must match an Authorized Redirect URI in Google Console
GOOGLE_REDIRECT_URI=http://localhost:3000/api/auth/google/callback

# ── Slack OAuth (for per-workspace bot installs) ──────────────────────────────
# Create at api.slack.com/apps → OAuth & Permissions
# Bot Token Scopes: commands, chat:write, channels:history
SLACK_CLIENT_ID=your-slack-client-id
SLACK_CLIENT_SECRET=your-slack-client-secret
SLACK_REDIRECT_URI=http://localhost:3000/api/auth/slack/callback
# Signing secret for verifying slash command payloads
SLACK_SIGNING_SECRET=your-slack-signing-secret

# ── App URLs ──────────────────────────────────────────────────────────────────
FRONTEND_URL=http://localhost:3000
